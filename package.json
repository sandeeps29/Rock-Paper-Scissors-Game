{
  "name": "rock-paper-scissors-game",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "homepage": "http://sandeep29.github.io/Rock-Paper-Scissors-Game",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf build && mkdir build && cpy 'index.html' 'build' && cpy 'style.css' 'build' && cpy 'script.js' 'build' && cpy 'rock-paper-scissors-images/**/*' 'build/rock-paper-scissors-images'",
    "predeploy":"npm run build",
    "deploy":"gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
